This folder is left empty intentionally, because maps are binary files which can get quite large and clutter the git repo. The current LARICS map collection for navigating FER floors and corridors can be downloaded from TODO: add maps

