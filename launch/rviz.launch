<launch>
  <arg name="config"/>

  <!-- rviz for visualization and goal publishing-->
  <node pkg="rviz" type="rviz" name="rviz"
        args="-d $(find p3dx_config)/rviz/$(arg config).rviz"/>
</launch>
